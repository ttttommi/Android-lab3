<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="20dp">

    <!-- Відображення всіх записів -->
    <TextView
        android:id="@+id/textView_data"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:text="Дані зі сховища з'являться тут" />

    <!-- Поле для введення ID -->
    <EditText
        android:id="@+id/editText_id"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введіть ID запису" />

    <!-- Поле для введення нової мови -->
    <EditText
        android:id="@+id/editText_language"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:hint="Введіть нову мову" />

    <!-- Кнопка оновлення запису -->
    <Button
        android:id="@+id/button_update"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Оновити запис" />

    <!-- Кнопка видалення запису -->
    <Button
        android:id="@+id/button_delete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Видалити запис" />

    <!-- Кнопка повернення назад -->
    <Button
        android:id="@+id/button_back"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Назад" />

</LinearLayout>